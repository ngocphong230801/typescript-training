{
  "name": "practice",
  "version": "1.0.0",
  "description": "Todo List TypeScript - A web application with HTML/CSS & Typescript",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -rf build && rm -rf .parcel-cache && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.9.3"
  },
  "dependencies": {
    "pnpm": "^8.9.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
